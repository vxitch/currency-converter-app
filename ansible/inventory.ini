# Since Ansible runs ON the production server targeting localhost,
# use a local connection. If you run Ansible from a separate control
# node instead, replace with the production server's IP and SSH key.

[production]
localhost ansible_connection=local ansible_user=ubuntu
